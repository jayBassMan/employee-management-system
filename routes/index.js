// ---------------BOILER PLATE-----------------------
const express = require("express");
// import random file
const randomfile = require("./randomFile");

const app = express();

app.use("/randomeFile", raondFile);

module.exports = app;
